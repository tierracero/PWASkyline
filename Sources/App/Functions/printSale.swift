//
//  DELETEME
//  
//
//  Created by Victor Cantu on 6/2/22.
//

import Foundation
import TCFundamentals
import JavaScriptKit
/*
func printSale(_ data: [SaleProductPDVObject],_ cust: CustPrintData){
    
    var custData = ""
    
    do {
        let _data = try JSONEncoder().encode(cust)
        
        if let str = String(data: _data, encoding: .utf8) {
            custData = str
        }
        
    } catch  {
        
    }
    
    do {
        let _data = try JSONEncoder().encode(data)
        if let jsonString = String(data: _data, encoding: .utf8) {
             _ = JSObject.global.printPdv!(jsonString,custData)
        }
        else{
            showError(.errorGeneral, "No se pudo iniciar impresion, Contacte a SoporteTC [001]")
        }
    }
    catch{
        showError(.errorGeneral, "No se pudo iniciar impresion, Contacte a SoporteTC [001]")
    }
}
*/
